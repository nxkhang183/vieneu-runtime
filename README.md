# VieNeu Runtime

This public repository contains immutable, verified VieNeu runtime release assets for compatible host applications.

Release assets include platform-specific CPU runtimes, a shared pinned model archive, checksums, compatibility metadata, and third-party notices. Application source code is maintained separately and is not published here.
